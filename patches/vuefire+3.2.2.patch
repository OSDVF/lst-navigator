diff --git a/node_modules/vuefire/dist/server/index.mjs b/node_modules/vuefire/dist/server/index.mjs
index 05ebc4c..59b6fa8 100644
--- a/node_modules/vuefire/dist/server/index.mjs
+++ b/node_modules/vuefire/dist/server/index.mjs
@@ -1,4 +1,12 @@
-import { getApps, initializeApp, cert, applicationDefault } from 'firebase-admin/app';
+import { createRequire } from 'node:module'
+const require = createRequire(import.meta.url)
+const {
+  initializeApp: initializeAdminApp,
+  cert,
+  getApps: getAdminApps,
+  applicationDefault,
+} = require('firebase-admin/app')
+
 import { consola } from 'consola';
 import { getAppCheck } from 'firebase-admin/app-check';
 import { initializeAppCheck, CustomProvider } from 'firebase/app-check';
@@ -14,7 +22,7 @@ const vuefireConsola = consola.withTag("vuefire");
 const FIREBASE_ADMIN_APP_NAME = "vuefire-admin";
 function ensureAdminApp(firebaseAdminOptions, name = FIREBASE_ADMIN_APP_NAME) {
   vuefireConsola.debug(`Checking if admin app "${name}" exists...`);
-  if (!getApps().find((app) => app.name === name)) {
+  if (!getAdminApps().find((app) => app.name === name)) {
     const {
       // these can be set by the user on other platforms
       FIREBASE_PROJECT_ID,
@@ -36,7 +44,7 @@ function ensureAdminApp(firebaseAdminOptions, name = FIREBASE_ADMIN_APP_NAME) {
     });
     if (FIREBASE_CONFIG || FUNCTION_NAME) {
       vuefireConsola.debug(`Using FIREBASE_CONFIG env variable for ${FUNCTION_NAME}`);
-      initializeApp(void 0, name);
+      initializeAdminApp(void 0, name);
     } else {
       let credential;
       if (GOOGLE_APPLICATION_CREDENTIALS) {
@@ -75,7 +83,7 @@ function ensureAdminApp(firebaseAdminOptions, name = FIREBASE_ADMIN_APP_NAME) {
         `Initializing Admin App "${name}" with options:`,
         firebaseAdminOptions
       );
-      initializeApp(
+      initializeAdminApp(
         {
           // TODO: is this really going to be used?
           ...firebaseAdminOptions,
@@ -85,7 +93,7 @@ function ensureAdminApp(firebaseAdminOptions, name = FIREBASE_ADMIN_APP_NAME) {
       );
     }
   }
-  return getApps().find((app) => app.name === name);
+  return getAdminApps().find((app) => app.name === name);
 }
 
 function VueFireAppCheckServer(app, adminApp, firebaseApp, {
